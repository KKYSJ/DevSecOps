version: "3.9"
services:
  backend:
    image: ${ECR_BACKEND_IMAGE:-secureflow/backend:latest}
    env_file: [.env]
  worker:
    image: ${ECR_WORKER_IMAGE:-secureflow/worker:latest}
    env_file: [.env]
  frontend:
    image: ${ECR_FRONTEND_IMAGE:-secureflow/frontend:latest}
