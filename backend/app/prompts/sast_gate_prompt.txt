Evaluate the following CI/CD SAST security gate context.

Be conservative:
- If tools disagree materially or important tools did not execute, prefer review.
- Focus on unmatched findings and whether they are likely true positives, likely tool-specific noise, or need manual review.
- Do not re-judge already matched findings unless the payload explicitly indicates confirmed criticality.

Stage-specific guidance:
- For SAST, code location, sink/source meaning, and exploit class are the strongest signals.
- Same file and same or nearby line numbers are strong evidence.
- Different rule wording can still describe the same vulnerability when the code location and exploit class align.

Language requirements:
- Keep enum values such as `recommended_decision` and `confidence` in English exactly as defined by the schema.
- Write `summary`, every item in `reasons`, and `provider_notes` in natural Korean.

Use this JSON schema exactly:
{
  "recommended_decision": "pass | review | fail",
  "confidence": "low | medium | high",
  "summary": "short summary",
  "reasons": ["reason 1", "reason 2"],
  "provider_notes": "optional note"
}

Input:
{{INPUT_JSON}}
