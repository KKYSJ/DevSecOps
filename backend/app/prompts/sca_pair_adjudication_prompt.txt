Review the following pre-matched SCA finding pairs and decide whether each pair should remain TRUE_POSITIVE, be downgraded to REVIEW_NEEDED, or be treated as FALSE_POSITIVE.

Focus on:
- whether both findings refer to the same package and version
- whether CVE or GHSA identifiers align
- whether the findings describe the same library risk and affected dependency

Write `title_ko`, `risk_summary`, `reason`, and `action_text` in Korean.
Return JSON only, with no markdown.

Pairs:
{{MATCHED_PAIRS_TEXT}}

Use this JSON schema exactly:
{{OUTPUT_SCHEMA_JSON}}
